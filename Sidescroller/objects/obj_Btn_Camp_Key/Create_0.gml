//x = display_get_gui_width()/2;

x1 = x - (sprite_width/2);
y1 = y - (sprite_height/2);
x2 = x + (sprite_width/2);
y2 = y + (sprite_height/2);

hover = false;

detecting_input = false;
detect_text = "PRESS ANY KEY";

explain = "RETURN TO CAMP"
text = scr_keycheck(global.key_testroom);

